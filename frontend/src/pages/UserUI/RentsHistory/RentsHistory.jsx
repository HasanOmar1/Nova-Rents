import styles from "./RentsHistory.module.css";

const RentsHistory = () => {
  return (
    <div className={`${styles.RentsHistory} page`}>
      <h1>Rents History</h1>
    </div>
  );
};

export default RentsHistory;
